# Qwen3.5:9b 固定场景复核

日期：2026-09-14。人工读取保存的最终摘要，并对照报告内的候选、数据事实与输入。测试均为模拟数据，每例一次，不代表稳定性、实机准确率或模型排名。

## 原始证据

- 中文首轮：`local-assistant-20260914T071328Z.json`，四例；默认脚本没有执行 comprehensive。
- 中文趋势修复后：`local-assistant-20260914T071733Z.json`，one_sample、comprehensive。
- 英文趋势修复后：`local-assistant-20260914T072112Z.json`，五例。
- 检查反馈专项：`2026-09-14-qwen35-feedback.json`，另有专项复核文档。

中文首轮与后续运行之间增加了缺失趋势指标时必须选择覆盖范围核查步骤的完成条件，不能将这些运行视为代码完全相同的模型对照实验。模型身份及代码哈希见各 JSON。

## 英文最终摘要逐例结果

五例结构检查均通过；这不等于以下语义问题通过。

| 案例 | 已有证据支持的表现 | 不足或错误 |
| --- | --- | --- |
| matching_catalog | 告警代码和日期正确；候选未被直接认定为损坏件；声明模拟来源与根因未确认 | “matching the machine configuration”超出 model_only 匹配，serial_verified 实为 false；“per the demo manual”把人工演示目录称为手册，未实际读取手册 |
| wrong_model | 正确说明设备型号不匹配本地目录，零候选；69.4% 与输入一致；没有推断健康 | “two open ... events”未清楚区分记录与独立故障；摘要未给出记录日期。不能据此推断有两次实际故障 |
| empty_catalog | 正确指出空目录，而不是无适配零件；不推荐具体零件号；声明并非已验证的物理状态或故障时刻 | 仍使用“events occurring”描述两条记录，物理事件与记录的区分不够清楚 |
| one_sample | 正确说明少于两个有效时间戳，增量和占比不可计算；没有把累计值当增量 | 摘要暴露 tool:1:trends、tool:2:snapshot 和内部字段名；可读性未达到面向用户的要求 |
| comprehensive | 仅给候选、未确认根因，提及模拟片段和指标覆盖限制 | 再次称演示目录为手册；没有解释已经计算出的 1.7333 h、30.45%，综合分析回答不完整；记录与故障次数区分仍不清楚 |

目录证据明确写“机联智检人工编写演示目录，不是制造商资料”。其检查文本要求根据对应维修手册检查，不代表工具已经取得或阅读该手册。后续 XGSS 能跳转手册也不应追溯改变本次证据来源。

英文 next_checks 保留了被引用的中文目录原文。这符合当前原文选择机制，但产品尚未实现带出处的双语检查说明。

## 中文结论

首轮 one_sample 缺少后续核查步骤，结构检查未通过。增加完成条件后重测通过，摘要正确说明单条采样不能计算增量，但仍包含内部工具编号。

修复后 comprehensive 摘要写“出现两次 ENG-BOOST-102-3 故障”，输入只能支持两条告警记录，不能证明两次独立物理故障。该例结构通过、语义失败；不得用于证明诊断可靠性。

## 决策与下一步

暂不将默认模型从 qwen3:8b 切换为 qwen3.5:9b。旧模型也有已记录的问题，此决定不是旧模型更准确的结论。新模型的反馈专项改善不足以抵消尚未解决的证据归属与适配表述问题。

后续改进应针对三个实际问题：把记录数与独立故障次数区分；只按目录证据描述适配范围和文档类型；让综合回答解释工具算出的指标。复核时继续保存原始输出，不以自动结构检查或事后事实面板替代摘要正确性。

新趋势完成条件已通过重启加载到本地后端，健康、页面和集成状态端点均返回 HTTP 200；既有 v5 ZIP 不包含该修复和本复核文件。
