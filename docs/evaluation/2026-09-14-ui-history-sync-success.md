# 网页真实历史同步成功路径

2026-09-14 在本地工作区“数据与备件库”页面，使用已有授权配置和已登记设备，选择最近 7 天，点击“同步历史数据”。未修改凭据或请求权限，没有调用故障事件接口。

实际页面状态由“正在读取”转为“已导入 36 条记录并选中该数据集”。当前设备选择切换到 XE55U 实测历史，来源显示“导入实测 / 未验证”。核验表显示：

- 累计工时：HTTP 200、36 条记录。
- 累计怠速：HTTP 200、0 条，明确显示返回空数据。
- 故障事件：未核验、记录数未知。

页面核验时间为本机时间 2026-09-14 02:02:38；它是这次设备和时段的查询结果，不能外推全车队或之后的接口状态。未重复点击或绕过 15 分钟限制。真实设备序列号、账户配置和原始响应不写入本公开验证说明。

这项证据补齐此前只有命令行成功、网页限流分支的缺口。它不证明故障事件读取可用，也不证明怠速为零或设备无故障。返回记录相同可能复用已有内容哈希数据集，不把这次查询宣称为新增测量数据。
