# v8 软件原型交付核验

日期：2026-09-14。软件包已完成独立解压、新虚拟环境安装及进程内应用启动检查。验收范围是本机演示和软件包完整性；整个项目仍未完成业务验收。

## 交付文件

- [v8 ZIP](../../dist/jilian-assistant-gemini-prototype-20260914-v8.zip)，28,840,769 字节，531 个条目，其中 530 个文件由清单逐项校验。
- [安装与演示说明](../RELEASE_V8_GUIDE.md)、[本版功能状态](../RELEASE_V8_NOTES.md)。压缩包根目录也有 `START_HERE.md`。
- [机器可读验收记录](2026-09-14-release-v8.json)、[SHA-256 文件](../../dist/jilian-assistant-gemini-prototype-20260914-v8.zip.sha256)。

SHA-256：`a1407af41b2d28448cd43fe4b1e25b2e6804c7da462f332dfa3b300d09604184`。
后端版本：`20260914.9-gemini-release`。候选包通过检查后按相同字节复制到最终路径，没有重新构建或覆盖旧版本。哈希用于完整性核验，不是作者数字签名。

最终交付时只读核对现有 `8892/assistant/runtime`，实际返回同一后端版本、`gemini` 和 `gemini-flash-latest`。未重启服务或调用模型；当前开发预览与包内版本一致。

## 实际检查结果

在 `.tmp/release-v8-clean-01/jilian-assistant` 创建全新 `.venv`，用包内 `setup_local.ps1 -PythonExecutable <Python 3.12.14 路径>` 安装固定版本运行依赖、生成空白 `.env` 和模拟案例。没有沿用项目开发虚拟环境。`pip check` 返回 `No broken requirements found.`。这是同一台电脑上的独立环境；部分依赖使用 pip 已有缓存，不代表另一台电脑或离线安装通过。

2026-09-14 10:23:30 UTC，包内 `scripts/release_smoke.py` 通过。它确认导入的应用和存储路径属于解压副本，运行真实 FastAPI 启动与关闭流程，并阻止非本机网络连接；没有新建监听服务，没有向云端发起请求。

| 检查 | 实际结果 |
| --- | --- |
| 空白 Gemini 配置 | 本地演示就绪，云端分析未就绪；不声称鉴权通过 |
| 模拟设备与故障资料 | 5 台明确模拟设备、1 份初始模拟案例，按故障查得 `DEMO-BOOST-SENSOR` 演示候选 |
| 趋势导出 | CSV 61 行，含 UTF-8 BOM |
| 工况识别 | 7 个独立测试片段可用，检查回放结果 |
| 冷却预警 | 24 个测试片段可用；预警交接生成对应模拟设备的 136 条截止时点以内观测，核验上下文一致 |
| 无密钥发起分析 | HTTP 503，明确失败，不创建成功诊断记录 |
| 安装和运行后完整性 | 530 个清单文件内容保持不变；运行数据写入单独目录 |

源码完整测试为 Python **336 项通过**（6 条既有弃用警告）、Node **18 项通过**，JavaScript 语法检查通过。最终 ZIP 的文件清单、模型及数据资源、ECharts 许可证、模拟设备关联、界面资源版本均通过检查；包内 Markdown 本地链接无缺失。未为同一结果重复调用云端或重复运行会新增数据的自检。

## 包含内容与未完成事项

包含当前网页和后端、扩展源码、模拟案例、完整冷却实验数据与评测标签、工况模型数据、训练与验收脚本、安装说明和第三方许可证。配置默认为 Gemini、空白密钥、模拟数据及关闭自动同步。包内没有真实设备缓存、用户 `.env`、私人目录或诊断记录。模拟身份统一使用 `SIM-` 编号。

冷却模型只预测模拟热事件，独立测试误报率约 0.268 次/有效观测小时，高于温度对照；不能据此声称已具备现场故障预测效果。详情见 [冷却预警说明](../COOLING_WARNING.md)。

Gemini 完整诊断与反馈尚未通过，最近实际请求为每日免费额度耗尽；本次没有重试或换模型。XGSS 生产手册所需参数及身份、正式备件资料、实际扩展安装、另一台实体电脑安装、现场效果仍待验收。v7 Proposal 早于冷却功能，旧答辩稿和视频仍需同步；本 ZIP 不包含这些旧材料。

## 复现

按 [安装说明](../RELEASE_V8_GUIDE.md) 解压到新目录并运行包内 setup，再使用 `-DemoOnly` 查看本地演示。完整性命令为 `.venv/Scripts/python.exe scripts/verify_release_v8.py --folder .`。仅在全新解压、空白示例配置且无自有数据的副本中运行 `scripts/release_smoke.py`，因为自检会添加明确的模拟测试数据。
