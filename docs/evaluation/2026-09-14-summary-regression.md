# 当前本地模型语义复核

模型 qwen3:8b；各案例单次运行，均为人工模拟输入。本记录不是现场准确率或稳定性统计。

初始结果 `local-assistant-20260914T054940Z.json` 四例结构检查通过，但人工阅读发现将运行状态误述为正常、未查询趋势却声称计数无异常、摘要重复检查步骤等问题。

修复 `app/local_assistant.py` 中被插入反馈说明打断的禁止编造条款，明确运行/在线状态及累计计数不能证明正常或没有异常。加强模拟回放措辞。中间两次结果 `055028Z` 和 `055124Z` 保留在本目录：前者仅一例通过，后者三例通过，失败均包含模型在 finish 时漏填摘要、引用或检查字段，最终达到步数上限。没有将失败当成成功或删除失败记录。

进一步将传给模型的 JSON Schema 全部决策属性列为必填，保留内部 Pydantic 默认值兼容现有调用。不得仅凭 action=finish 省略其他字段；运行后的语义及引用校验继续保留。`tests/test_decision_schema.py` 检查实际发给模型的 required 集合。

最终 `local-assistant-20260914T055210Z.json` 四例结构检查通过。人工复核：

- 正确目录：返回演示候选并说明模拟来源，没有确认真实根因；摘要仍重复目录检查步骤。
- 错误型号：没有错误候选，说明目录未匹配；未明确解释机型不匹配这一具体过滤原因，摘要也没有再次强调模拟来源。
- 空目录：没有候选，未确认根因；摘要没有明确重述模拟来源。
- 单采样点：没有编造工时增量或怠速比例，但将不足原因写成未覆盖完整运行周期、怠速数据缺失，未准确区分存在单条计数与缺少可计算增量的时间区间。

本次没有再出现“运行状态正常”的表达，仅能说明这四次输出有改善，不能保证其他输入不再发生。自然语言语义质量仍未全部通过。全套自动测试为 164 passed、6 个已有弃用警告；自动测试不覆盖全部上述语义问题。
