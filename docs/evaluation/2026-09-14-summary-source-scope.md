# 摘要来源与适配范围改进

## 修改及验证范围

在 app/local_assistant.py 的现有摘要指令中明确：model_only 只表示设备型号列入目录；人工演示目录不能当成已读取的维修手册；故障记录数不是独立故障次数；趋势任务需解释已计算的指标或不可计算原因。没有新增自动改写摘要、放宽检查步骤要求或取消六步上限。

流程与输出格式相关测试：50 passed（tests/test_local_assistant.py、tests/test_decision_schema.py）。这仅验证原有控制流程，不证明自然语言摘要正确。另执行以下真实本地模型案例，每例一次，输入均为模拟数据。

| 模型/案例 | 原始文件 | 观察 |
| --- | --- | --- |
| Qwen3.5:9b 英文目录匹配 | local-assistant-20260914T072841Z.json | 结构通过；不再称目录为手册，明确 model-only 和配置待核实；仍使用含糊的 two open instances |
| Qwen3.5:9b 英文综合 | 同上 | 结构通过；写 two open fault records，指标 1.7333 h、0.5278 h、30.45% 与工具一致；型号匹配限制明确。未充分强调有效区间不等于完整覆盖，不计为所有语义要求通过 |
| Qwen3.5:9b 中文综合 | local-assistant-20260914T072933Z.json | 结构通过；开始解释指标，明确模拟来源和型号匹配限制；仍暴露内部工具 ID，使用“首次发生”“最后一次”等含糊的事件措辞 |
| Qwen3:8b 英文综合 | local-assistant-20260914T073019Z.json | 结构通过，但写 recurring fault、fault is still active，超出历史记录能证明的状态；没有解释趋势数值 |
| Qwen3:8b 中文综合 | local-assistant-20260914T073058Z.json | 未完成：两次 finish 都漏选目录检查步骤，达到原六步上限。失败原样保留，未降低完成要求 |

窗口证据均为 07:01–08:45 UTC、1.7333 h，104 个有效区间、无排除区间；完整性仍为 not_verified。该单一片段不能验证一般工况或失效预测。

## 本机模型选择

基于本轮同代码下的上述观察，以及此前五类场景与检查反馈专项结果，将本机 .env 的 OLLAMA_MODEL 改为已安装的 qwen3.5:9b，并重启本项目后端加载摘要修改。此前复核中的“暂不切换”是当时的决策，本次在获得当前默认模型的新失败证据后调整。

这属于本机原型的工作配置选择，不是模型稳定性排名或正式诊断验收。新模型仍有已记录的语义与可读性问题，所有 AI 摘要继续标为待复核。仓库通用默认配置和既有 v5 ZIP 未改，旧包不会自动获得本机配置；若后续发布需重新统一配置和依赖说明。

实际后端调用另保存为 2026-09-14-qwen35-local-backend.json；以该文件的 HTTP 状态、model 和 history_saved 字段判定是否接入成功，不以 .env 写入成功推断调用成功。

实际后端验证已完成：HTTP 200、model=qwen3.5:9b、history_saved=true。再从历史接口读取报告，摘要和数据事实均与本次响应一致，返回原文支持的检查步骤。记录 ID 为 e7998fcce2c7dd94e2aff8da31d3530964be073cffc3d1238ac28c17a14b6016。

该次摘要准确引用 30.45%，说明仅型号适配、重复记录不等于独立故障、未检索维修手册，并保留模拟来源。仍使用“两条事件分别发生”等不够精确的记录措辞；此次改善不证明所有输入已满足语义要求。
