# 数据不足时保留后续步骤

Qwen3.5 9B 中文初测 local-assistant-20260914T071328Z.json 中，one_sample 正确解释单一时间戳无法计算窗口指标，但 next_checks 为空，评测因此失败；没有把这项失败改为通过。

现已在助手完成条件中要求：明确趋势任务且存在 check:trend-coverage 选项时，必须选择该项。适用于不可计算指标或存在排除区间的情况。模型漏选时收到纠正消息，仍不满足则受原六步上限约束而失败；不代填模型未选择的检查，不编造维修步骤。

新增两个流程回归覆盖首次漏选后改正和持续漏选失败；原计数计算测试补充合法检查选择，保留计算断言。完整套件 235 passed、6 条既有弃用警告（包括本机可用的可选 XGSS 加密测试）。

实际新模型复测 local-assistant-20260914T071733Z.json：one_sample、comprehensive 均通过结构检查。单样本例现在返回原文数据核查步骤，正确保留不可计算结论；摘要仍含内部工具名称，属于可读性缺陷。综合例仍把两条故障记录表述为“两次故障”，且没有充分解释已经查询的趋势数值，语义审查不能全部通过。

已核实本地 8890 监听进程的 Python 路径及 uvicorn 启动命令，随后重启本项目后端以加载修复。重启后 /health、/assistant-ui/、/assistant/integration-status 均返回 HTTP 200；health 显示 mock 数据源。此项是服务启动检查，不代替上述实际模型流程复测。v5 ZIP 不含后续改动。应用默认模型仍为 qwen3:8b，Qwen3.5 为独立评测进程。英文五例复核见 2026-09-14-qwen35-bilingual-review.md。
